1. 기존의 코드가 알수 없는 오류를 나타내어 방법을 확인하고 여러번 수정했으나 해결되지 않아, 코드를 다시 짰습니다.
2. desired값이 변하지 않는 문제가 있었으나, 사물에 정책을 연결하듯, 규칙에도 정책을 연결해야 한다는 것을 알아 문제를 해결했습니다.
3. publish된 state.reported값을 이용하여 desired값을 바꿉니다. desired는 온도는 적당한지, 먹이는 제대로 주어졌는제, 물을 가는 주기가 되었는지 등을 확인하고,
이상이 있을 경우 ESP32로 이상값을 전달하여, ESP32가 이상값을 전달 받으면 해당하는 부분의 조정기능을 시작합니다.
4. desired값을 esp32로 처음 시작할때 전송하여 값을 초기화 할수 있으나, 규칙같은 경우는 sql을 수정할 수 없어 esp32에서 현재 state값을 쏘면, desire값에 에러표시가 나올 경우,
lambda를 통해 email로 조치를 취하는 것을 권고하는 메일을 보냅니다. 메일은 계속해서 보내집니다.(조건 변경 가능)

구현 성공
1. 일정 시간마다 자동으로 aws iot에 publish하고, 중간에 웹페이지를 요청한다면, 웹페이지를 반환하고 그곳에서도 임의로 publish를 수행할 수 있습니다